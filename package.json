{
  "name": "redhead-web",
  "version": "0.1.0",
  "private": true,
  "description": "Local web tool for official document red-heading, rule maintenance, and PDF preview.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --port 3721",
    "docs:rules": "node scripts/generate-rule-coverage-doc.js",
    "test": "node tests/run-tests.js",
    "test:word": "node tests/run-tests.js --word"
  },
  "engines": {
    "node": ">=18"
  }
}
